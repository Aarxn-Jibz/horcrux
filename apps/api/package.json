{
  "name": "@ciphermesh/api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir dist",
    "typecheck": "tsc --noEmit --pretty false",
    "db:migrate:local": "wrangler d1 migrations apply ciphermesh --local"
  },
  "dependencies": {
    "@ciphermesh/shared": "workspace:*",
    "hono": "^4.9.6",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250906.0",
    "wrangler": "^4.34.0"
  }
}

