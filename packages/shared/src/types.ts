export type ObjectKind = "shard" | "key-share";
export interface ObjectPlacement { id: string; kind: ObjectKind; index: number; nodeId: string; endpoint?: string; objectId: string; size: number; checksum: string; shardType?: "data" | "parity"; status: "pending" | "stored" | "missing" | "corrupt" }
export interface FileManifest { fileId: string; originalName: string; mimeType: string; originalSize: number; compressedSize: number; encryptedSize: number; plaintextHash: string; ciphertextHash: string; encryptionAlgorithm: "AES-256-GCM"; compressionAlgorithm: "zstd"; encryptionIv: string; dataShards: number; parityShards: number; shardSize: number; keyShareThreshold: number; keyShareCount: number; objects: ObjectPlacement[] }
export interface FileSummary extends Omit<FileManifest, "objects"> { status: "uploading" | "available" | "failed" | "deleted"; createdAt: string }
export interface StorageNodeContract { id: string; publicIdentifier: string; name: string; endpoint?: string; status: "online" | "offline" | "degraded" | "disabled"; storageCapacity: number; storageUsed: number; availableStorage?: number; lastSeen: string | null; nodeVersion?: string | null; protocolVersion?: string | null; health?: "healthy" | "degraded" | "unknown"; kind?: "laptop" | "browser-mock" }
